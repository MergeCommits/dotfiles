- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: dots/gitconfig

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - 
    command: zsh wakeup.sh
    stdout: true